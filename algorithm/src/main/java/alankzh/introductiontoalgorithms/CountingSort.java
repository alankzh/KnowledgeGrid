package alankzh.introductiontoalgorithms;

/**
 * 计数排序
 * 假定输入都是在0到k区间内的一个整数。
 * 计数排序的思想是，对每一个输入元素x，确定小于x的元素个数，
 * 利用这一信息，可以把x放到输出数组中应该所处的位置。
 *
 * 总时间复杂度为O(k + n), 当k = O(n) 时，就可以选择计数排序
 */
public class CountingSort {



}
